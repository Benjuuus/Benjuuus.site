import {sites} from '../pages/api/sites';

export default function Partnership(){
    return(
        <>
            {sites?.map(el=>{
                return(<>

                </>)
            })}
        </>
    )
}